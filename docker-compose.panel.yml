version: "3.9"

services:
  app:
    build: .
    container_name: tg-signer-panel
    environment:
      - APP_HOST=0.0.0.0
      - APP_PORT=3000
      - PORT=3000
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    restart: unless-stopped



